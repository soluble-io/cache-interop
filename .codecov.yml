coverage:
  status:
    patch: off
    project:
      default: off
      cacheInterop:
        target: 1%
        flags:
          - cacheInterop
flags:
  e2e:
    paths:
      - packages/cache-e2e-tests
  cacheInterop:
    paths:
      - packages/cache-interop

ignore:
  - "scripts/**/*"
  - "docs/**/*"

comment:
  layout: "reach,diff,flags,tree"
  behavior: default
  require_changes: no
